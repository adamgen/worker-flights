import { MinuteDurationPipe } from './minute-duration.pipe';

describe('MinuteDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new MinuteDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
